# LightKV 代码审查未修复问题清单

> 审查日期：2026-07-23
> 已修复：44 项 | 未修复：1 项（P2×1）
> 本文档记录需要后续版本处理的性能优化问题。

---

## 已修复（本轮新增）

| # | 严重度 | 问题 | 修复方式 |
|---|--------|------|----------|
| 1 | P2 | SkipList 热路径堆分配 | 新增 string_view 重载，避免每次操作创建 std::string |
| 2 | 🔴 P0 | `DBImpl::Get` 忽略 `ReadOptions::snapshot_seq` | 使用 `options.snapshot_seq` 非 0 时优先，修复事务隔离性 |
| 3 | 🔴 P0 | WAL 写入失败重试导致重复记录 | 删除重试循环，直接返回错误 |
| 4 | 🔴 P0 | `BlockCache::Erase` 锁顺序死锁风险 | 先收集 key 再释放 file_index_mutex_，后调用 cache_.Erase |
| 5 | 🔴 P0 | 迭代器释放锁后 SkipList 悬垂指针 | `SkipListSourceIterator` 持有 `shared_ptr<const MemTable>` 保持存活 |
| 6 | 🟠 P1 | L1+ `Get` 线性扫描 | 改用 `std::lower_bound` 二分查找 |
| 7 | 🟠 P1 | CRC32C 查找表重复定义 | 提取共享 `detail::kCrc32cTable`，`Crc32c` 委托给 `Crc32cExtend` |
| 8 | 🟠 P1 | Compaction 中 `oldest_snapshot_` 只读一次 | 添加 `SetSnapshotProvider` 回调，每 1000 条记录刷新一次 |
| 9 | 🟡 P2 | Manifest `snprintf` 缓冲区溢出 | 替换为 `std::ostringstream`，无长度限制 |
| 10 | 🟡 P2 | `Abandon` 留下孤立文件 | 添加 `filename()` 访问器，`Abandon` 中自动删除文件 |
| 11 | 🟡 P2 | `generate_uuid` 格式字符串可移植性 | 替换为 `std::ostringstream` + `std::hex` |

---

## 未修复（性能优化，需重大架构变更）

### 1. ZSet 所有操作全量扫描排序 O(N log N)

- **文件**: `src/server.cpp:2601-2631` (`zscan_all`)
- **现象**: `ZRANGE`、`ZREVRANGE`、`ZCOUNT`、`ZRANK` 等所有 ZSet 命令都调用 `zscan_all` 进行全量 Scan + 内存排序。时间复杂度 O(N log N)，对大 ZSet（如排行榜百万级成员）极其低效。
- **影响**: 性能瓶颈，ZSet 操作延迟随数据量线性增长。
- **当前实现**: KV 编码存储（score_key → member, member_key → score），依赖 Scan + 排序。
- **修复方向**: 引入内存跳表或平衡树作为 ZSet 索引，支持 O(log N) 的 rank/range 操作。
- **复杂度**: 高（需要重新设计 ZSet 存储结构，修改所有 ZSet 命令，处理持久化）。
- **建议**: 作为 v2.0 版本的重大特性开发。

---

## 备注

- 当前实现在中小数据量（<10 万成员）下表现良好。
- 仅在大数据量（>100 万成员）的排行榜场景下需要优化。
- 可考虑引入第三方库（如 LevelDB 的 SkipList）作为 ZSet 索引。
